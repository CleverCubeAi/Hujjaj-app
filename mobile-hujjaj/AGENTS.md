# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

This app targets **Expo SDK 54** so it opens in store Expo Go (App Store / Play Store are frozen at SDK 54). Do not upgrade to SDK 55+ unless you switch to a custom Expo Go or a development build.
